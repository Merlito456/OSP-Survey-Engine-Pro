import React from 'react';

export function MapOverlay() {
  return React.createElement(
    'div',
    {
      style: {
        width: '100%',
        height: '100%',
        background: '#e5e7eb',
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center',
        fontSize: 14,
        fontWeight: 700,
        color: '#334155'
      }
    },
    'MapOverlay (stub – Leaflet disabled)'
  );
}
